Built using GT-Helper (unreleased version).

After cloning, run `npm run watch` to build the dist folder,
add your API key to a .env file, and your space URL to the 
/config/config.js and run `npm run start` to launch the extension.
